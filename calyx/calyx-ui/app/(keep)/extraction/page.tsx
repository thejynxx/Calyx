import Extraction from "./extraction";

export default function Page() {
  return <Extraction />;
}

export const metadata = {
  title: "Calyx - Event Extraction",
  description:
    "Map new event attributes from existing event attributes with Regex",
};
